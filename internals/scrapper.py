from objects import Page, Form, Field, Cookie


class Scrapper:
    @staticmethod
    def scrap(page_code: str) -> Page:
        # get data
        # construct Page object
        # return it
        pass
